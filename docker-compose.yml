services:
  family-menu-bot:
    build: .
    image: family-menu-bot:latest
    container_name: family-menu-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TZ: Europe/Berlin
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
